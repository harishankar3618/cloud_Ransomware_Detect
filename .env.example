APP_NAME="Ransomewatch"
APP_ENV=local
# Run: php artisan key:generate to get your unique key
APP_KEY=base64:d5Mrh7MWm2fnBwBIs2ypgK7v/heUniCjtiSnKQAJqSA=
APP_DEBUG=true
APP_URL=http://localhost:8000

LOG_CHANNEL=stack
LOG_DEPRECATIONS_ENABLED=true
LOG_LEVEL=debug

# --- Database Configuration ---
# Your project uses SQLite by default, which needs no credentials.
DB_CONNECTION=sqlite
# If you decide to use MySQL, uncomment and configure these:
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=cloud_ransomware_db
# DB_USERNAME=root
# DB_PASSWORD=

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
# Use 'redis' for better performance, but 'sync' or 'database' is fine for development.
QUEUE_CONNECTION=sync 
SESSION_DRIVER=file
SESSION_LIFETIME=120

# --- Mail Configuration ---
# IMPORTANT: These settings are for Laravel's general mail functionality.
# The Python script (Project/email_alert.py) uses its own hardcoded Gmail settings.
MAIL_MAILER=log # Set to 'log' to prevent accidental emails during development
MAIL_HOST=
MAIL_PORT=
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="security-alert@ransomewatch.com"
MAIL_FROM_NAME="${APP_NAME}"

# --- Frontend (Vite) Configuration ---
VITE_APP_NAME="${APP_NAME}"

# No Pusher/AWS config needed for basic functionality
# ... (rest of the keys can be left as is)